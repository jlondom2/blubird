import React from "react";
import { Link } from "react-router-dom";

export const Step1 = ({ register_signup_fields, formState }) => {
  const { isDirty, isValid, errors2 } = formState;
  return <></>;
};
